{
    "hooks": {
        "SessionStart": [
            {
                "type": "command",
                "command": "echo '[hive] Web Dev session started' && node --version && python3 --version",
                "description": "Log session start with runtime versions"
            }
        ],
        "Stop": [
            {
                "type": "command",
                "command": "echo '[hive] Session stopped'",
                "description": "Log session stop"
            }
        ]
    }
}
