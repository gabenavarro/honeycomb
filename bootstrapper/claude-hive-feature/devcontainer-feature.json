{
  "id": "claude-hive",
  "version": "0.4.0",
  "name": "Claude Hive Agent",
  "description": "Installs Claude Code CLI and the hive-agent WebSocket client for Claude Hive orchestration",
  "options": {
    "projectType": {
      "type": "string",
      "enum": ["base", "ml-cuda", "web-dev", "compbio"],
      "default": "base",
      "description": "Project type determines which skills and CLAUDE.md template are installed"
    },
    "installSkills": {
      "type": "boolean",
      "default": true,
      "description": "Install curated skills for the project type"
    },
    "hubUrl": {
      "type": "string",
      "default": "http://host.docker.internal:8420",
      "description": "URL of the Claude Hive hub"
    },
    "hiveAgentSource": {
      "type": "string",
      "default": "git+https://github.com/gabenavarro/honeycomb.git@main#subdirectory=hive-agent",
      "description": "pip-installable source for hive-agent. Override when vendoring a fork or pinning to a specific SHA."
    }
  },
  "installsAfter": [],
  "entrypoint": "",
  "customizations": {
    "vscode": {
      "extensions": ["anthropic.claude-code"]
    }
  }
}
